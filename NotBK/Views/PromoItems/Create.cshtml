@model NotBK.Models.PromoItem

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>PromoItem</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CodItem" class="control-label"></label>
                <select asp-for="CodItem" class ="form-control" asp-items="ViewBag.CodItem"></select>
            </div>
            <div class="form-group">
                <label asp-for="CodPromo" class="control-label"></label>
                <select asp-for="CodPromo" class ="form-control" asp-items="ViewBag.CodPromo"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
